PORT=5000
JWT_SECRET=your_super_secret_key_change_this_in_production
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# PostgreSQL (for production — replace in-memory store)
DATABASE_URL=postgresql://user:password@localhost:5432/flexswap

# Redis (optional — for caching rates)
REDIS_URL=redis://localhost:6379

# Paystack (Nigerian payment gateway)
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxx

# Exchange rate API (for live USD/NGN rate)
FX_API_KEY=your_exchange_rate_api_key
